using Xunit;

namespace TennisApp.Tests;

public class ProjectStructureTests
{
    [Fact]
    public void Project_Should_Build_Successfully()
    {
        // This test verifies that the project builds
        // The fact that this test runs means the build succeeded
        Assert.True(true);
    }
}
